/**
 * @mainpage QPlainText
 *
 * QPlaintext is a plain text editor library which depends only on Qt.
 * It is meant to allow a consistent API for plugins across editors that
 * use the library while allowing editors to customize the library's appearence
 * and functionality as they see fit.
 *
 */

